{
  "name": "web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:api": "nodemon --watch dev-server --ext mjs --exec \"node dev-server/server.mjs\"",
    "dev:full": "run-p dev:api dev",
    "build": "vite build",
    "preview": "vite preview",
    "sync": "npm run build && node ./scripts/sync-dist.mjs"
  },
  "devDependencies": {
    "nodemon": "^3.1.11",
    "npm-run-all": "^4.1.5",
    "vite": "^7.2.2"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
